import styled from 'styled-components';

export default function Footer() {

    const FooterInner = styled.footer`
        background: #191E2E;
        height: 100px;
    `

    return (
        <FooterInner />
    );
}
